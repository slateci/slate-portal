all:
  children:
    dev:
      hosts:
        portal:
          ansible_host: portal-dev.slateci.io
#    prod:
#      hosts:
#        portal:
#          ansible_host: portal.slateci.io
  vars:
    ansible_ssh_common_args: '-o ProxyCommand="ssh -W %h:%p -q <slate-user>@bastion.slateci.net -i <path>"'
    ansible_ssh_private_key_file: '<path>'
    ansible_user: '<slate-user>'
