all:
  children:
    <group>:
      hosts:
        portal:
          ansible_host: <host>
  vars:
    ansible_ssh_common_args: '-o ProxyCommand="ssh -W %h:%p -q <slate-user>@bastion.slateci.net"'
    ansible_user: '<slate-user>'
