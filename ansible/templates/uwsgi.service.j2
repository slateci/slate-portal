[Unit]
Description=uWSGI
After=syslog.target network.target

[Service]
Type=simple
User=centos
ExecStart=/usr/sbin/uwsgi --ini /etc/slate/slate.ini --die-on-term
Restart=always

[Install]
WantedBy=multi-user.target
