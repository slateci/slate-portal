---
- name: Add nginx HTTPS configuration
  template:
    src: "templates/nginx-https.conf.j2"
    dest: "/etc/nginx/nginx.conf"
    owner: root
    group: root
    mode: 0644
    force: yes

- name: Restart nginx.service
  service:
    name: nginx
    state: restarted
